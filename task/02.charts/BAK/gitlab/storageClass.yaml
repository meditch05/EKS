gitlab:
  gitaly:
    persistence:
      storageClass: aws-efs
      size: 50Gi
postgresql:
  persistence:
    storageClass: aws-efs
    size: 8Gi
minio:
  persistence:
    storageClass: aws-efs
    size: 10Gi
redis:
  master:
    persistence:
      storageClass: aws-efs
      size: 5Gi

